{
  "name": "chenxing-td",
  "version": "11.0.0",
  "private": true,
  "description": "晨星守望 · 像素风塔防 Roguelite（单文件网页）",
  "scripts": {
    "build": "bash build.sh",
    "check": "bash build.sh && node tests/syntax.js",
    "test": "bash build.sh && node tests/syntax.js && node tests/run-all.js",
    "test:mobile": "mkdir -p tests/out && cd tests/out && node ../mobile-landscape.js",
    "sim": "mkdir -p tests/out && cd tests/out && node ../sim.js"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
